# hello_package

A simple Flutter package that says hello 👋

## Features

- Provides a simple method `sayHello()` that returns a greeting message.

## Getting started

Add this line in your `pubspec.yaml`:

```yaml
dependencies:
  hello_package: ^1.0.0
